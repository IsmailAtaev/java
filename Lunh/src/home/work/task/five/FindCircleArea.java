package home.work.task.five;

public class FindCircleArea {
    private double r;
    private double s;

    public double radius(double length) {
        return length / (2 * Math.PI);
    }

    public double square(double r) {
        return Math.PI * r * r;
    }
}